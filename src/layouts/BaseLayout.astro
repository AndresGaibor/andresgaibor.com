---
import '../styles/global.css';
import Header from '../components/layout/Header.astro';
import Footer from '../components/layout/Footer.astro';
import { profile } from '../data/profile';
import { social } from '../data/social';

interface Props {
  title?: string;
  description?: string;
  socialDescription?: string;
  noindex?: boolean;
  socialImage?: string;
  socialImageAlt?: string;
}

const {
  title = `${profile.name} — Software Engineer`,
  description = profile.summary,
  socialDescription,
  noindex = false,
  socialImage = '/social-card-v2.jpg',
  socialImageAlt = `${profile.name} — Software Engineer · Full Stack · Data Engineering`,
} = Astro.props;

const socialDescriptionContent = socialDescription ?? description;
const siteUrl = Astro.site ?? new URL(profile.site);
const canonical = new URL(Astro.url.pathname, siteUrl);
const socialImageUrl = new URL(socialImage, siteUrl);
const socialImageType = socialImageUrl.pathname.endsWith('.png')
  ? 'image/png'
  : socialImageUrl.pathname.endsWith('.webp')
    ? 'image/webp'
    : 'image/jpeg';
const fullTitle = title.includes(profile.name) ? title : `${title} — ${profile.name}`;
const structuredData = {
  '@context': 'https://schema.org',
  '@type': 'Person',
  name: profile.name,
  url: profile.site,
  jobTitle: 'Software Engineer',
  sameAs: [social.github, social.x],
};
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <meta name="description" content={description} />
    {noindex && <meta name="robots" content="noindex,nofollow" />}
    <link rel="canonical" href={canonical} />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="alternate" type="application/rss+xml" title={`Blog de ${profile.name}`} href="/rss.xml" />

    <meta property="og:type" content="website" />
    <meta property="og:site_name" content={profile.name} />
    <meta property="og:title" content={fullTitle} />
    <meta property="og:description" content={socialDescriptionContent} />
    <meta property="og:url" content={canonical} />
    <meta property="og:image" content={socialImageUrl} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:image:type" content={socialImageType} />
    <meta property="og:image:alt" content={socialImageAlt} />

    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:site" content="@andres_gaibor" />
    <meta name="twitter:title" content={fullTitle} />
    <meta name="twitter:description" content={socialDescriptionContent} />
    <meta name="twitter:image" content={socialImageUrl} />
    <meta name="twitter:image:alt" content={socialImageAlt} />

    <script is:inline type="application/ld+json" set:html={JSON.stringify(structuredData)} />
    <title>{fullTitle}</title>
  </head>
  <body>
    <a href="#contenido" class="sr-only focus:not-sr-only focus:fixed focus:left-4 focus:top-4 focus:z-[100] focus:bg-[var(--surface)] focus:px-4 focus:py-2">Saltar al contenido</a>
    <Header />
    <main id="contenido">
      <slot />
    </main>
    <Footer />
  </body>
</html>
